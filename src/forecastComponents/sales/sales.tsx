

export default function Sales() {
  return (
            
      <div className="w-full border-primary-2 border-double border-2 rounded-lg p-4 md:p-8">
              <h2 className="text-primary-0 w-full font-semibold text-xl text-center pb-8">{`Qualification & Conversions`}</h2>
              <p>{`Stage conversions -- SWOT x territory/segment (where did we see our quickest, higher dollar conversions?)`}</p>
              <p>{`DQs x stage - can we find out timing/budget/use case earlier?`}</p>
            </div>

  );
}
